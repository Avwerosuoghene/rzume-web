export enum IconStat {
  success =  'done',
  failed = 'close'
}

export enum EmailValMsg {
  validating = 'Please wait while your email is being validated',
  confirm = 'Kindly check your email for the validation link'
}

export enum EmailValidHeaderMsg {
    validating = 'Validating',
    validated = 'Email Confirmed',
    error = 'Error',
    confirm = 'Confirm Mail'
}

export enum ButtonTxt {
  continue = 'Continue',
  resendValidation = 'Resend Validation'
}

export enum GenericMsg {
  validating = 'Validating',
  expiredSession = 'User Session Expired'
}

export enum QueryParams {
  token = 'token'
}

export enum ErrorMsges {
  unknown = 'An Unknown Error Occured'
}
